#include <Wire.h> 
#include <LiquidCrystal_I2C.h>

// Alterar o endereço conforme modulo I2C
LiquidCrystal_I2C lcd(0x27, 2,1,0,4,5,6,7,3, POSITIVE); 

#define BotA 2
#define BotB 3
#define BotC 4
#define BotD 5

int nQuestao = 10;
int array[10];
int ponto = 0;
  
void setup(){
  pinMode(BotA, INPUT);
  pinMode(BotB, INPUT);
  pinMode(BotC, INPUT);
  pinMode(BotD, INPUT);
  
  lcd.begin(16, 2);
  lcd.clear();
  lcd.setCursor(0,0);
  
  lcd.print("Seja Bem Vindo!");
  Serial.begin(9600);
  delay(2000);
  lcd.clear();
  
  geraSequencia();
}

void loop(){
ponto = 0;
  for (int i = 0; i < nQuestao; i++){
    SelecionaQuestao(array[i]);
  }
  
  lcd.clear();
  lcd.setCursor(0,0);
  lcd.print("Fim de Jogo!");
  
  delay(2000);
}


void SelecionaQuestao(int nQuestao){
  
  switch(nQuestao){
    case 0:
      lcd.clear();
      lcd.setCursor(0,0);
      lcd.print("Quem fez o papel do Comandante Cody?
	A) Jay Laga'aia B) Temuera Morrison
	C) Melhor Ahmed D) Joel Edgerton");
	for(int i=0; i<=20; i++){
        lcd.scrollDisplayLeft();
      }
    if(AnalisaBotao()=='B'){
          lcd.setCursor(0,1);
          lcd.print("Correto!");
        }else{
          lcd.setCursor(0,1);
          lcd.print("Errado!");
        }
    break;
    case 1:
      lcd.clear();
      lcd.setCursor(0,0);
      lcd.print("Onde as Guerras Clônicas começaram?
	A) Tatooine B) Genosis
	C) Naboo D) Coruscant");
	for(int i=0; i<=20; i++){
        lcd.scrollDisplayLeft();
      }
      if(AnalisaBotao()=='B'){
          lcd.setCursor(0,1);
          lcd.print("Correto!");
        }else{
          lcd.setCursor(0,1);
          lcd.print("Errado!");
        }
    break;
    case 2:
      lcd.clear();
      lcd.setCursor(0,0);
      lcd.print("Qual é a arma de escolha de Chewbacca?
	A) Rifle blaster B) Sabre de luz
	C) Clube de metal D) Bowcaster");
	for(int i=0; i<=20; i++){
        lcd.scrollDisplayLeft();
      }
        if(AnalisaBotao()=='D'){
          lcd.setCursor(0,1);
          lcd.print("Correto!");
        }else{
          lcd.setCursor(0,1);
          lcd.print("Errado!");
        }
    break;
    case 3:
      lcd.clear();
      lcd.setCursor(0,0);
      lcd.print("Quem disse esta citação: “Eu escolho viver para o meu povo.”
	A) Padmé Amidala B) Riyo Chuchi
	C) Rainha Jamillia D) Hera Syndulla");
	for(int i=0; i<=20; i++){
        lcd.scrollDisplayLeft();
      }
        if(AnalisaBotao()=='B'){
          lcd.setCursor(0,1);
          lcd.print("Correto!");
        }else{
          lcd.setCursor(0,1);
          lcd.print("Errado!");
        }
    break;
    case 4:
      lcd.clear();
      lcd.setCursor(0,0);
      lcd.print("O que Luke Skywalker perdeu em sua luta com Darth Vader?
	A) A mão esquerda B) O pé esquerdo
	C) A mão direita D) Perna esquerda ");
	for(int i=0; i<=20; i++){
        lcd.scrollDisplayLeft();
      }
        if(AnalisaBotao()=='C'){
          lcd.setCursor(0,1);
          lcd.print("Correto!");
        }else{
          lcd.setCursor(0,1);
          lcd.print("Errado!");
        }
    break;
    case 5:
      lcd.clear();
      lcd.setCursor(0,0);
      lcd.print("Que tipo de veículo a Rey usa como casa?
	A) AT-ST B) Star Destroyer
	C) Mon Calimari D) AT-AT");
	for(int i=0; i<=20; i++){
        lcd.scrollDisplayLeft();
      }
        if(AnalisaBotao()=='D'){
          lcd.setCursor(0,1);
          lcd.print("Correto!");
        }else{
          lcd.setCursor(0,1);
          lcd.print("Errado!");
        }
    break;
    case 6:
      lcd.clear();
      lcd.setCursor(0,0);
      lcd.print("Quem matou o Jabba?
	A) Han Solo B) C-3PO
	C) Luke Skaywalker D) Princesa Leia");
	for(int i=0; i<=20; i++){
        lcd.scrollDisplayLeft();
      }
        if(AnalisaBotao()=='D'){
          lcd.setCursor(0,1);
          lcd.print("Correto!");
        }else{
          lcd.setCursor(0,1);
          lcd.print("Errado!");
        }
    break;
    case 7:
      lcd.clear();
      lcd.setCursor(0,0);
      lcd.print("Quem matou Jango Fett?
	A) Mance windu B) Anakin Skaywalker
	C) Obi-Wan Kenobi D) Yoda);
	for(int i=0; i<=20; i++){
        lcd.scrollDisplayLeft();
      }
        if(AnalisaBotao()=='A'){
          lcd.setCursor(0,1);
          lcd.print("Correto!");
        }else{
          lcd.setCursor(0,1);
          lcd.print("Errado!");
        }
    break;
    case 8:
      lcd.clear();
      lcd.setCursor(0,0);
      lcd.print("Qual era a espécie do Yoda?
	A) Desconhecida B) Yordle
	C) Yangral D) Yolor");
	for(int i=0; i<=20; i++){
        lcd.scrollDisplayLeft();
      }
        if(AnalisaBotao()=='A'){
          lcd.setCursor(0,1);
          lcd.print("Correto!");
        }else{
          lcd.setCursor(0,1);
          lcd.print("Errado!");
        }
    break;
    case 9:
      lcd.clear();
      lcd.setCursor(0,0);
      lcd.print("Em que ano foi a primeira aparição do BB-8?
	A) 2010 B) 2015
	C) 2020 D) 2018");
	for(int i=0; i<=20; i++){
        lcd.scrollDisplayLeft();
      }
        if(AnalisaBotao()=='B'){
          lcd.setCursor(0,1);
          lcd.print("Correto!");
        }else{
          lcd.setCursor(0,1);
          lcd.print("Errado!");
        }
    break;
    case 10:
      lcd.clear();
      lcd.setCursor(0,0);
      lcd.print("Quem se ofereceu pra liderar o ataque contra a Estrela da Morte?
	A) Han Solo B) Nien Numb
	C) Lando Calrisisian D) Lulu");
	for(int i=0; i<=20; i++){
        lcd.scrollDisplayLeft();
      }
      if(AnalisaBotao()=='C'){
          lcd.setCursor(0,1);
          lcd.print("Correto!");
        }else{
          lcd.setCursor(0,1);
          lcd.print("Errado!");
        }
    break;
    case 11:
      lcd.clear();
      lcd.setCursor(0,0);
      lcd.print("Qual a espécie do Chewbacca?
	A) Rodian B) Wookie
	C) Ewok D) Abednedo");
	for(int i=0; i<=20; i++){
        lcd.scrollDisplayLeft();
      }
      if(AnalisaBotao()=='C'){
          lcd.setCursor(0,1);
          lcd.print("Correto!");
        }else{
          lcd.setCursor(0,1);
          lcd.print("Errado!");
        }
 break;
    case 12:
      lcd.clear();
      lcd.setCursor(0,0);
      lcd.print("O que os Ewoks pensaram que o C-3PO era?
	A) Um amigo B) Um demônio
	C) Um Deus D) Um fantasma");
	for(int i=0; i<=20; i++){
        lcd.scrollDisplayLeft();
      }
      if(AnalisaBotao()=='C'){
          lcd.setCursor(0,1);
          lcd.print("Correto!");
        }else{
          lcd.setCursor(0,1);
          lcd.print("Errado!");
	}
	break;
    case 13:
      lcd.clear();
      lcd.setCursor(0,0);
      lcd.print("Qual o nome de batismo do Kylo Ren?
	A) John B) Ben
	C) Caio D) Paulo");
	for(int i=0; i<=20; i++){
        lcd.scrollDisplayLeft();
      }
      if(AnalisaBotao()=='B'){
          lcd.setCursor(0,1);
          lcd.print("Correto!");
        }else{
          lcd.setCursor(0,1);
          lcd.print("Errado!");
	}
	
	break;
    case 14:
      lcd.clear();
      lcd.setCursor(0,0);
      lcd.print("Quem dirigiu "O Retorno de Jedi"?
	A) George Lucas B) Irvin Kirshir
	C) Gabriel Rosa D) Richard Marquand");
	for(int i=0; i<=20; i++){
        lcd.scrollDisplayLeft();
      }
      if(AnalisaBotao()=='D'){
          lcd.setCursor(0,1);
          lcd.print("Correto!");
        }else{
          lcd.setCursor(0,1);
          lcd.print("Errado!");
	}
	break;
    case 15:
      lcd.clear();
      lcd.setCursor(0,0);
      lcd.print("Qual o planeta natal do Darth Vader?
	A) Yavin 4 B) Terra
	C) Saturno  D) Tatooine");
	for(int i=0; i<=20; i++){
        lcd.scrollDisplayLeft();
      }
      if(AnalisaBotao()=='D'){
          lcd.setCursor(0,1);
          lcd.print("Correto!");
        }else{
          lcd.setCursor(0,1);
          lcd.print("Errado!");
	}
	break;
    case 16:
      lcd.clear();
      lcd.setCursor(0,0);
      lcd.print("Qual o planeta natal do Han Solo?
	A) Corellia B) Marte
	C) Primus D) Saturno");
	for(int i=0; i<=20; i++){
        lcd.scrollDisplayLeft();
      }
      if(AnalisaBotao()=='A'){
          lcd.setCursor(0,1);
          lcd.print("Correto!");
        }else{
          lcd.setCursor(0,1);
          lcd.print("Errado!");
	}
break;
    case 17:
      lcd.clear();
      lcd.setCursor(0,0);
      lcd.print("Quantas torres tem o Templo Jedi?
	A) 17 B) 2
	C) 9 D) 5");
	for(int i=0; i<=20; i++){
        lcd.scrollDisplayLeft();
      }
      if(AnalisaBotao()=='D'){
          lcd.setCursor(0,1);
          lcd.print("Correto!");
        }else{
          lcd.setCursor(0,1);
          lcd.print("Errado!");
	}
  }
  
}

char AnalisaBotao(){
  bool BotaoPressionado = true;
  
  while(BotaoPressionado){
    if(digitalRead(BotA)){
      BotaoPressionado = false;
      return 'A';
    }else if(digitalRead(BotB)){
      BotaoPressionado = false;
      return 'B';
    }else if(digitalRead(BotC)){
      BotaoPressionado = false;
      return 'C';
    }else if(digitalRead(BotD)){
      BotaoPressionado = false;
      return 'D';
    }
  }
}



void geraSequencia(){
  randomSeed(random(0, 1000));
  for (int i = 0; i < nQuestao; i++){     
    array[i] = i;
  }
 
  for (int i = 0; i < nQuestao; i++) {    
    int temp = array[i];
    int novoIndice = random(nQuestao);
    array[i] = array[novoIndice];
    array[novoIndice] = temp;
  }
}
